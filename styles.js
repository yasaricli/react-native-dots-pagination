import { StyleSheet } from "react-native";

export default (Styles = StyleSheet.create({
  container: {
    justifyContent: 'center', 
    alignItems: 'center'
  },

  passive: { },
  active: { }
}));